# FastAPI and server (Production-ready versions)
fastapi>=0.103.0,<0.120.0  # Latest stable with security updates
uvicorn[standard]>=0.24.0,<0.30.0
python-multipart>=0.0.6,<0.1.0

# Database (SQLAlchemy 2.x with latest security patches)
sqlalchemy>=2.0.23,<2.1.0
alembic>=1.13.0,<1.15.0
psycopg2-binary>=2.9.9,<3.0.0  # PostgreSQL driver
aiosqlite>=0.19.0,<1.0.0  # Async SQLite driver for development
asyncpg>=0.29.0,<1.0.0  # Async PostgreSQL driver

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-decouple==3.8
PyJWT>=2.8.0,<3.0.0  # JWT token handling for Supabase
supabase>=2.3.0,<3.0.0  # Supabase Python client

# AI and ML
openai==1.3.5
transformers>=4.35.2,<5.0.0  # Python 3.12 compatible
torch>=2.1.2,<3.0.0  # Updated for Python 3.12 wheels
scikit-learn>=1.4.0,<2.0.0  # Python 3.12 compatible
textstat==0.7.3
spacy>=3.7.2,<4.0.0  # Updated for compatibility

# HTTP requests
httpx==0.25.2
requests==2.31.0

# Payment Processing
# paddle-billing==1.0.1  # Package not available, disabled for now

# Data processing
pandas==2.1.4
numpy>=1.26.2,<2.0.0  # Python 3.12 compatible wheels

# PDF generation
reportlab>=4.0.7,<5.0.0  # Python 3.12 compatible
weasyprint>=60.1,<61.0  # Updated for compatibility

# Email
fastapi-mail==1.4.1
resend==0.7.0  # Production email service
jinja2>=3.1.2,<4.0.0  # Email template rendering

# CORS
# fastapi-cors==0.0.6  # Use built-in FastAPI CORS middleware instead

# Validation (Pydantic V2 with settings)
pydantic>=2.5.0,<3.0.0  # V2 with latest security updates
pydantic-settings>=2.1.0,<3.0.0  # Required for BaseSettings

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Monitoring and Logging
psutil>=5.9.6,<6.0.0  # System monitoring for health checks
prometheus-client>=0.19.0,<1.0.0
sentry-sdk[fastapi]>=1.40.0,<2.0.0
structlog>=23.2.0,<24.0.0
colorama>=0.4.6,<1.0.0

# Background tasks
celery==5.3.4
redis==5.0.1
tenacity>=8.2.0,<9.0.0  # Retry logic for database connections

# Blog system dependencies
python-frontmatter==1.0.1
python-slugify==8.0.1
Markdown==3.5.1
markdown-it-py==3.0.0
markdown2==2.4.10
pymdown-extensions==10.7
Pillow>=10.2.0,<11.0.0  # Python 3.12 compatible
